baseURL = '/'
languageCode = 'en-us'
title = 'New Single Out Now!'
theme = "hugo-theme-hello-friend-ng-master"

PygmentsCodeFences = true
PygmentsStyle = 'monokai'

copyright = 'This work is licensed under a Creative Commons Attribution-NonCommercial 4.0 International License.' # This message is only used by the RSS template.

archetypeDir = 'archetypes'
contentDir   = 'content'
dataDir      = 'data'
layoutDir    = 'layouts'
publishDir   = 'public'

buildDrafts  = false
buildFuture  = false
buildExpired = false
canonifyURLs = true

enableRobotsTXT = true
enableGitInfo   = false
enableEmoji     = true
enableMissingTranslationPlaceholders = false
disableRSS     = false
disableSitemap = false
disable404     = false
disableHugoGeneratorInject = false

[pagination]
  disableAliases = false
  pagerSize = 20
  path = 'page'

[params]
  dateform        = "Jan 2, 2006"
  dateformShort   = "Jan 2"
  dateformNum     = "2006-01-02"
  dateformNumTime = "2006-01-02 15:04"

  sender_script = '''
  <script>
    (function (s, e, n, d, er) {
      s['Sender'] = er;
      s[er] = s[er] || function () {
        (s[er].q = s[er].q || []).push(arguments)
      }, s[er].l = 1 * new Date();
      var a = e.createElement(n),
          m = e.getElementsByTagName(n)[0];
      a.async = 1;
      a.src = d;
      m.parentNode.insertBefore(a, m)
    })(window, document, 'script', 'https://cdn.sender.net/accounts_resources/universal.js', 'sender');
    sender('d687ea1a565178')
  </script>
  '''

  # Metadata mostly used in document's head
  # 
  description = 'Brock Bushong Official Website'
  keywords = 'music'
  images = ['img/brocklogo.png']

  # Home subtitle of the index page.
  #
  # homeSubtitle = ''

  # Set a background for the homepage
 backgroundImage = "img/brocklogo.png"

  # Set disableReadOtherPosts to true in order to hide the links to other posts.
  #
  disableReadOtherPosts = true

  # Enable theme toggle
  # 
  # This options enables the theme toggle for the theme. 
  # Per default, this option is off. 
  # The theme is respecting the prefers-color-scheme of the operating system. 
  # With this option on, the page user is able to set the scheme he wants. 
  enableThemeToggle = true

  # Sharing buttons
  #
  # There are a lot of buttons preconfigured. If you want to change them,
  # generate the buttons here: https://sharingbuttons.io
  # and add them into your own `layouts/partials/sharing-buttons.html`
  #
  enableSharingButtons = true

  # Global language menu
  #
  # Enables the global language menu.
  #
  enableGlobalLanguageMenu = false

  # Integrate Javascript files or stylesheets by adding the url to the external assets or by
  # linking local files with their path relative to the static folder, e.g. "css/styles.css"
  #
  customCSS = []
  customJS  = []

  # Toggle this option need to rebuild SCSS, requires extended version of Hugo
  #
  justifyContent = false  # Set "text-align: justify" to .post-content.


  [params.author]
    name = "Brock Bushong"

  # Custom footer 
  # If you want, you can easily override the default footer with your own content. 
  #
  [params.footer]
    trademark = 2024
    rss = false
    copyright = false
    author = true

    topText = []
    bottomText = [
      "Made with &#10084; by <a href=\"https://juliya.dev\">Juliya Gannon</a>"
    ]

  # Colors for favicons
  #
  [params.favicon.color]
    mask = "#1b1c1d"
    msapplication = "#1b1c1d"
    theme = "#1b1c1d"

  [params.logo]
    logoMark     = "+"
    logoText     = "Brock Bushong"
    logoHomeLink = "/"
    # Set true to remove the logo cursor entirely.
      logoCursorDisabled = true
    # Set to a valid CSS color to change the cursor in the logo.
    # logoCursorColor    = "#67a2c9"
    # Set to a valid CSS time value to change the animation duration, "0s" to disable.
    # logoCursorAnimate  = "2s"
    # Append the current url pathname to logoText 
    # logoCursorPathname = true

  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #  
  # Addon: Startpage portrait 
  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
  # Uncomment this if you want a portrait on your start page
  #
  # [params.portrait]
  #   path     = "img/brock06.jpg"
  #   alt      = "Brock Bushong"
  #   maxWidth = "150px"

 
  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #  
  # Addon: Social icons
  # # # # # # # # # # # # # # # # # # # # # # # # # # # # #
  #
  [[params.social]]
    name = "instagram"
    url  = "https://www.instagram.com/brockbushong/"

  [[params.social]]
    name = "email"
    url  = "mailto:nobody@example.com"

  [[params.social]]
    name = "spotify"
    url  = "https://open.spotify.com/artist/456FhEMFevDKnSmw0klw9G"

  [[params.social]]
    name = "facebook"
    url  = "https://www.facebook.com/brockbushongmusic?mibextid=LQQJ4d"

  [[params.social]]
    name = "tiktok"
    url  = "https://www.tiktok.com/@brockbushong1?_t=8p4hGZj6J8k"

[languages]
  [languages.en]
    weight    = 1
    copyright = '<a href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank" rel="noopener">CC BY-NC 4.0</a>'
  [languages.en.params]
    subtitle  = "Brock Bushong"

  [languages.fr]
    weight    = 2
    copyright = '<a href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank" rel="noopener">CC BY-NC 4.0</a>'
  [languages.fr.params]
    subtitle  = "Brock Bushong"

[menu]
  [[menu.main]]
    identifier = "about"
    name       = "About"
    url        = "about/"
  [[menu.main]]
    identifier = "singles"
    name       = "Singles"
    url        = "singles/"